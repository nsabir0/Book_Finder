<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abir.diubookfinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderActivity"></activity>
        <activity android:name=".Shipment_infoActivity" />
        <activity android:name=".BooklistActivity" />
        <activity
            android:name=".UpdateUserActivity"
            android:label="@string/title_activity_update_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PurchaseActivity" />
        <activity android:name=".Bookinfo2Activity" />
        <activity android:name=".BookinfoActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".Login1Activity" />
        <activity android:name=".editBookActivity" />
        <activity android:name=".deleteBookActivity" />
        <activity android:name=".addBookActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".SellerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>